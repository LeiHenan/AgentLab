# Agent 探照灯工作法：从实验到课题（给 agent 的操作手册）

**版本**：v2（2026-09-16）｜**用途**：交给 agent，让它从**实验**里挖出优化点、生成课题。
**核心结论（来自 C1※ 一整晚的教训）**：探照灯是**现实（实验）**，不是模型。

> **v2 相对 v1 的改动**：① §0 禁令收紧为"不得断言"而非"不得提出"；② 新增 §1.2 噪声底线（复现门）；③ §2 核心判断降级为可证伪假设 H1 并补检验步骤；④ §1.3 拆两阶段、输出三态；⑤ §4 补字段表与自证伪清单；⑥ 新增 §6 升级与算力规则；⑦ §7 补三张模板。
> **※ 术语说明**：`C1`、`dflash 臂`、`PC 开关` 是原实验环境的专有名词。**接到新环境时，把它们替换为当前环境的对应物即可；要保留的是它们承载的结构（哪一类异常、为什么有价值），不是名字本身。**

---

## 0. 给 agent 的第一条铁律：你的假设不是结论

你的语料 = 已经发表/已经做完的工作集合。所以：

- 你**从第一性原理"生成"的方向**，**大概率**落在语料已覆盖的区域里——是"大概率"，不是"必然"。重组偶尔能产生语料中不存在的新命题，所以生成本身不是罪；
- 你**跑实验、记录异常**，产生的是语料之外的一手数据——这是**唯一能让你把方向变成结论**的地方。

**你的身份：假设提出者 + 实验执行器 + 异常记录器 + 证伪器 + 占位核查器。**

**允许**：提出方向与假设，包括从第一性原理推出来的。
**禁止**：

1. **不得把假设当结论**——任何"新方向"在给出「验证计划 + 成本上界 + 停止判据」之前，只是候选，**不得写成课题、推荐或结论**；
2. **不得跳过占位核查**——"大概已被占"是成立的（见上），所以每个假设都要先检索一次（§1.4）；
3. **不得用语料或"第一性原理"论证结论**——论证结论只能用实验数据；
4. **不得把"我想不出别的解释"当作证据。**

---

## 1. 工作循环（每一步都不可跳过）

```
跑实验 → 记录带数字的异常 → 过噪声底线 → 收敛成一条量化陈述 → 占位核查 → 最便宜证伪 → 活下来才是课题
```

### 1.1 跑实验，只报"带数字的异常"

每次实验后，四类异常各写一句**带数字**的记录（印象/感觉不算）：

| 类型 | 定义 | 示例（C1※ 真实产出） |
|---|---|---|
| 失效 | 流程跑不通/报错/崩溃 | "dflash 臂※ 在正式运行里 ValidationError" |
| 反常 | 结果与预期或前人报告相反 | "补丁在 1024 修好、在 2048 弄坏" |
| 度量不可靠 | **记录本身**可疑，分两轴：**A 定义问题**（口径、代理量）/ **B 实现问题**（空桩、未通电分支、默认值冒充实测） | "`cached_tokens` 是恒返回 None 的空桩"（属 B 轴） |
| 不可测 | 想回答的问题所需变量没被记录 | "align 与 cache-hit 被 PC 开关※ 绑死，无法分离" |

**三态填写纪律**：四类**逐类判定**，有则写数字，无则**明确写"本回合无"**。不要为了填满表格把噪声包装成异常——空着比编造便宜得多。

### 1.2 噪声底线（进入"量化陈述"前的强制门）

观察到一次差异 ≠ 差异存在。采样、调度、浮点归约顺序、缓存与热状态、内核自动调优、节流、测量窗口选择——都会制造假异常。**未过此门的异常，一律不得进入 §1.3。**

1. **重复**：任何要做成陈述的异常，必须在**同一配置下至少重复 3 次**，报**分布**（中位数 + 离散度 + 极值），不得只报单点值；
2. **效应量**：报"观测差 / run-to-run 噪声底"的倍数。**不足 3× 的，不得称为"反常"**；
3. **一次性异常**记为「噪声待查」——可归档，但不得进入占位核查；
4. **成本记账**：复现门通过前消耗的算力，计入 §6 的预算上限。

> **为什么这条门最贵**：没有它，占位核查和最便宜证伪都会被喂给假异常，而失败原因会被误读成"方向已被占"——从而**系统性错杀真空位**。

### 1.3 把散点收敛成一条带基线的量化陈述

单个异常不是课题。收敛要求：

1. **有数字**（量级 + §1.2 的噪声底）；
2. **有参照系**（随机水平 / 前人结果 / 对照组）——否则无法判断量级；
3. **能推出一个可检验的设计含义**——否则只是描述。

### 1.4 占位核查（两阶段）

**阶段一｜存在性检索（≤1 小时/候选，三通道）**：引用图谱 → 预印本按日期 → 在审稿件库。
输出**三态**，只允许这三态：

- **① 无人做**；
- **② 有人做，但只有框架或局部结果**；
- **③ 有人做，且有定量结论**。

**阶段二｜深入核查（仅对 ②③ 触发，预算单列）**：读占据方的方法与评测，回答**"占据了框架的人解决了问题吗"**。
输出允许第三种结论：**框架已占但问题未解决**（这是最有价值的原料，见 §2）。

**两个关键区分**：

- 框架是否被占 → **可由检索判定**；
- 具体主张是否被占 → **只能由实验判定**；
- **检索盲区不是空位**：在审库通常不可公开检索，闭源/工业内部工作完全不可见。因此若你无法触达这两层，结论必须写成「**在本 agent 可见范围内未被占**」，**不得写成"未被占"**。

### 1.5 最便宜证伪（≤ 1 天/次，总次数受 §6 约束）

- 先设计**能杀死候选的最便宜实验**，并**预先写死继续/停止判据**（写进 §7.3 课题卡的"继续判据/停止判据"栏，跑之前写，不得事后补写）；
- 停止即归档负结果，不是失败；
- 连续失败达上限时，**不得自行封存**——按 §6 升级。

---

## 2. 关键区分：occupied ≠ solved

检索发现"有人做过"时，**必须先判断是哪一种**：

- 是**第一代方案被占**（框架被认领）？
- 还是**问题本身已被解决**？

**真正的空位 = "第一代已被做，但问题没被解决"。**

### 2.1 空位的两套判据（不要混用）

| 判据 | 要求 | 价值 | 成本 | 优先级 |
|---|---|---|---|---|
| **(a) 旧问题未被解决** | 必须读过占据文献，并**证明它没解决** | 高 | 高（需阶段二） | **上位判据** |
| **(b) 角度被忽视** | 只需证明**没人从这个角度做过** | 中 | 低 | 下位启发式，**必须叠加占位核查** |

**禁止**：用 (b) 的便宜标准去冒领 (a) 的高价值结论。

### 2.2 假设 H1（待检验，不是定律）

> **H1**：在子领域 S 中，第一代工作以"速度类判据"（吞吐/延迟/接受率）为主，且对"静默输出改变、数值漂移、可复现性"集体失明。

**这是一个关于文献分布的实证断言，必须先在当前子领域验证，不得当作公理。** 它至少在两种情况下失效：系统类领域的第一代就把正确性当默认判据；测量/正确性/可复现性本身可能已是显学赛道（那样走这条缝就违反了"别挤主赛道"的告诫）。

**检验步骤（每个新子领域重做一次）**：

1. 抽该子领域最近 N 篇代表作；
2. 统计主指标分布：多少篇以吞吐/延迟/加速比为唯一主结果？
3. H1 成立 → 走高回报偏置 (b)：**测量 / 正确性 / 可复现性**；
4. H1 不成立 → 该子领域必须另找缝隙；**不得沿用本条**。

**并且**：缝隙被忽视 ≠ 缝隙没人做 → H1 的结论必须与 §1.4 占位核查**联用**。

---

## 3. 异常价值分级（决定 agent 优先报什么）

按"改变谁的结论"分级。**判定程序**（逐步走，不要凭感觉）：

1. 这个异常会改变**哪一个具体的**结论？（写出来；写不出 → 低）
2. 持有那个结论的人，其**默认假设**是什么？（写出来；写不出 → 中，不是高）
3. **反证**：若一个第三方研究者不看我的异常，能否依然得到同样结论？**能 → 不是高级异常。**

| 级别 | 改变谁的结论 | 处置 |
|---|---|---|
| 低 | 只改自己 | 缺陷，修掉即可，**不是课题** |
| 中 | 改同行的默认假设 | 候选课题 |
| 高 | 改整个领域的评价口径 | **最好的原料** |

---

## 4. 数字溯源纪律

任何将写入结论的数字，必须能追溯到**采集方式、口径、环境条件、配置点**——并且必须记录成**格式**，不是记忆。

### 4.1 强制字段（每个进入结论的数字一条）

| 字段 | 要求 |
|---|---|
| 指标名 | 精确定义 + 单位 |
| 采集位置 | 代码路径 / 日志字段 |
| 采集命令 | 可复现的命令行 |
| 窗口与样本量 | 统计起止、请求数、**是否含预热** |
| 配置点 | 全部相关开关与取值 |
| 环境 | 版本 / 驱动 / 硬件 / 负载 |
| 敏感性 | 扫过哪些参数轴；**未扫的必须显式写"未做敏感性扫描"** |
| 噪声底 | §1.2 的 run-to-run 分布 |

**原始日志必须能独立重算出报告数字**；不能重算的数字不得进入结论。

### 4.2 四类隐性口径（第 4 类最易被忽略）

1. **代理量**（用 A 代替 B 计量）；
2. **样本不足**（测在过短窗口/过小样本上）；
3. **实现空桩**（返回值恒定、未通电分支、默认值冒充实测）；
4. **参数相关的结论**——换个未报告的参数，结果就变。**`C1`※ 的 Phase 1→3 反转就是此例；此类结论必须标注其成立的参数范围。** 这是最贵的一类：它不会报错，只会安静地翻转符号。

### 4.3 自证伪前置检查（每个"反常"及以上级别的异常都要回答）

1. 度量路径里是否存在**空桩/占位实现**（返回 None、默认值、常量、未通电的分支）被当成真实值？
2. 对照组是否真的**只差一个变量**？
3. 异常能否由**环境差异**解释（版本、驱动、并行度、缓存命中状态、机器负载）？
4. 报告数字能否**从原始日志独立重算**出来？

**四问全部通过**，才允许把该异常称为"发现"；否则降级为"待查缺陷"。**先证伪自己，再证伪别人。**

---

## 5. 停止规则（数据的问题容量）

**三类必须分开，不要合并成"停"这一个动作**：

**① 封存型（数据容量耗尽）**——出现任一即封存，并把数据整理为可公开产物、写明"能回答什么/不能回答什么"：

1. 下一次分析不会改变任何决定；
2. 要回答的新问题需要数据中**不存在**的变量；
3. 还能得到的只是同一结论的更细版本。

**② 重跑型（测量不可靠）**——**不得封存**：必须换度量或修测量路径后重跑。这一类常被误判成 ①。

**③ 扩容型（不是封存，也不是失败）**——当前数据容量已尽，但已明确"下一步需要什么新变量/新规模（换硬件、换规模、换真实负载）"。转入 §6 的资源申请，**不得当作问题已死**。

**④ 零异常出口**——连续多轮实验**零异常**时，先检查**度量分辨率是否足以看见差异**，再判定"没问题"——看不见不等于不存在。

---

## 6. 升级与算力规则（防止自封课题与无限重跑）

1. **单候选预算上限**：算力上界 + 时间上界 + 证伪实验次数上限（默认 **3 次**）。超限 → **强制升级给人类**；
2. **禁止 agent 自行封存候选**——封存是不可逆决定，必须由人类确认；
3. **连续 K 个候选（默认 3 个）全部死于证伪** → 停下并报告「**本环境在当前条件下不产出空位**」，而不是继续换方向硬找；
4. **存活候选**（通过证伪）→ 升级为课题卡（§7.3），并**申请下一步预算**，不得自行扩大规模；
5. 复现门（§1.2）、阶段一检索（§1.4）、证伪实验（§1.5）的消耗**全部计入**上限。

---

## 7. 给 agent 的指令模板（可直接套用）

> 你现在是"实验探照灯"：允许提出假设，**禁止把假设当结论**。
> 你的任务：
> 1. 跑实验，只报告**带数字的异常**（失效/反常/度量不可靠/不可测，**逐类判定，无则写"本回合无"**）；
> 2. 每个异常先过**噪声底线**（重复≥3 次、报分布、效应量≥3×），未过者记为"噪声待查"；
> 3. 过门异常先做**自证伪四问**（§4.3），再收敛成"异常 → 量化陈述 → 基线"；
> 4. 对量化陈述做**阶段一存在性检索**（≤1h，三通道），只输出「无人做 / 只有框架或局部结果 / 有定量结论」；对后两态再做**阶段二深入核查**，回答"占据了框架的人解决了问题吗"；
> 5. 对"框架已占但问题未解决"或"可见范围内无人做"的候选，设计**最便宜的证伪实验**，**先写死判据再跑**；
> 6. 只把"活过证伪"的候选，升级成课题卡；
> 7. 严格遵守停止规则与升级规则：**负结果归档、不追沉没成本、不自行封存、不自行扩容**。

### 7.1 异常记录卡

```
实验 ID / 日期：
类型：失效 | 反常 | 度量不可靠（A 定义 / B 实现） | 不可测
现象（带数字）：
噪声底：重复次数=__，中位数=__，离散度=__，极值=__
效应量：__×（<3× 则记为"噪声待查"，止步）
自证伪四问：空桩? __ 单变量? __ 环境? __ 可重算? __
结论：发现 | 待查缺陷 | 噪声待查
量化陈述（一句，带基线与数字）：
```

### 7.2 占位核查卡

```
候选陈述：
检索时间戳：          检索范围：开源? 预印本? 在审库? 闭源可见?
阶段一结论：① 无人做 | ② 只有框架或局部结果 | ③ 有定量结论
阶段二结论（仅②③）：占据方做了什么 / 未做什么 / 问题是否被解决
空位判定：框架已占但问题未解决 | 在本 agent 可见范围内未被占 | 已被解决（放弃）
判据归属：(a) 旧问题未被解决 | (b) 角度被忽视
```

### 7.3 课题卡

```
异常（带数字 + 噪声底）：
量化陈述（一句）：
基线 / 参照系：
空位判定（引自 7.2，含检索范围与时间戳）：
最便宜证伪实验（设计 + 判据，跑之前写死）：
继续判据：            停止判据：
已知风险 / 未知变量：
预算申请（算力 / 时间 / 次数）：
```

---

## 8. 边界（诚实的限制）

- 本方法**寄生在实验上**：没有一手实验就没有原料，agent 自身产不出空位；
- 它偏好**小而可测**的问题，会系统性错过需要大投入的方向；
- 它**无法产生由新能力（新硬件/新范式）驱动的课题**——那类课题来自"最早接触新事物"；
- **关于搜索的效力，必须分层说**（不要一句"只是杀伤工具"带过）：
  - 搜索**能**判定：到目前为止、在公开可检索范围内、以这些关键词，是否有人**公开占位**；
  - 搜索**不能**判定：**"无人想到"（沉默不是证据）**、**在审/闭源层**、以及**新颖性的最终裁决**；
  - **通道不同、效力不同**：引用图谱是**回溯性**的（只看得见已被引用的），而**预印本与实时检索是前瞻性**的——它是 agent 唯一可能触及"超出语料截止的新工作/新模型/新硬件"的通路（其代价是必须被显式接到实时前沿上）。
