# 审核意见：《Agent 探照灯工作法：从实验到课题》

**被审文件**：`Agent探照灯工作法_从实验到课题_2026-09-16.md`（124 行，git 首次提交，无历史版本可对比）
**审核视角**：内部一致性、因果链完整性、可操作性（能否直接交给 agent 执行）、声明可靠性
**总体判断**：**骨架可用，方向正确，但有三处「必改」和若干处「该补」**。文档最有价值的部分是第 2 节的 `occupied ≠ solved` 与第 1.4 节的「先写死判据再跑」——这两条是真方法；最脆弱的部分是第 0 节的绝对禁令和第 2 节被当作定律的经验概括。

> **处置状态（2026-09-16 更新）**：本意见的 G1/G2/G3 + M1–M6 + L1/L2/L3/L4/L5/L7/L8 已落到 v2 —— `Agent探照灯工作法_从实验到课题_v2_2026-09-16.md`（269 行）。**L6（H1 的文献支撑）仅部分落地**：v2 的 §2.2 写了检验步骤但未附证据链接，建议把下附材料补进 §2.2 或在投稿/立项材料中引用。

**结论速览**

| 编号 | 问题 | 严重度 | 处置 |
|---|---|---|---|
| G1 | 「禁止无实验数据提出新方向」是无限禁令，误杀假设驱动型研究 | **必改** | 限定为「不得断言」而非「不得提出」 |
| G2 | 核心假设（「第一代只优化速度、三判据集体失明」）表述为定律，实为待证假设 | **必改** | 降级为可证伪假设 + 预注册 |
| G3 | 循环缺少「复现/噪声底线」环节，异常→量化陈述之间断链 | **必改** | 在第 1.2 节前插入复现门 |
| M1 | 空位来源有两个互相竞争的价值理论，且「正确性赛道」可能已是显学 | 该补 | 合并为一套带优先级的判据 |
| M2 | 「异常为真」需先自证伪；度量不可靠可自伤整个方法论 | 该补 | 新增前置证伪检查清单 |
| M3 | 占位核查「1 小时」与「是否被解决」不相容；输出二元结论与 §2 自相矛盾 | 该补 | 拆两阶段 + 三态输出 |
| M4 | 「搜索永远是杀伤工具，不是探照灯」既低估工具能力，又混淆工具与用途 | 该补 | 改为能力/盲区 + 效力分层 |
| M5 | 数字溯源无结构化字段，只有原则没有记录格式；§1.1「度量不可靠」定义过窄 | 该补 | 给字段表 + 定义拆两轴 |
| M6 | 缺少人工升级规则与整体成本核算 | 该补 | 追加「升级与算力规则」一节 |
| L1–L8 | 术语不统一（度量不可靠 / 隐性口径）、未定义专有名词（C1、dflash、PC 开关）、「各一条」逼供模板、缺课题卡模板、分级无判定程序、§2 全称量词无支撑、参数敏感性归类错位、缺少「实验做完但零异常」出口 | 可选 | 见第 3 节 |

---

## 1. 站得住的部分（建议保留，不要改）

1. **第 0 节否定「agent = 想法生成器」** 这个定位纠偏本身正确，是文档的立意所在（问题只在其强度，见 G1）。
2. **第 1.4 节「先设计能杀死候选的最便宜实验，并预先写死继续/停止判据」** —— 全文最硬的一条，等价于预注册，直接对抗事后合理化。
3. **第 2 节 `occupied ≠ solved`** —— 最关键的认知区分，且给出了可操作的追问顺序（是框架被占，还是问题已被解决）。
4. **第 3 节按「改变谁的结论」分级** —— 分级轴选得好，因为它直接对应价值而非工作量。
5. **第 4 节四要素溯源 + 三类隐性口径** —— 作为检查清单合格。
6. **第 5 节停止规则** —— 用「数据的问题容量」而非「进度」来决定封存，是正确的止损逻辑。
7. **第 7 节边界自述** —— 主动写明「寄生在实验上」「偏好小而可测」「无法产生新能力驱动的课题」，这种诚实度少见，应当保留并作为全文的信任基础。

---

## 2. 必改项

### G1｜第 0 节：绝对禁令误杀假设驱动型研究

**原文**：
> 你**从第一性原理"生成"新方向** = 重组合语料 = **必然产出已被占的东西**；
> **禁止**：在没有实验数据的情况下，凭"思考/第一性原理"提出任何"新方向"。

**问题（因果链断在第三步）**：`语料有限 → 生成即重组 → 重组必已被占`。前两步成立，第三步不成立。重组可以产生语料中不存在的新命题：把两个各自已存在的机制组合起来，可能得到谁也不曾提出的第三条。文档自身第 7 节也承认存在「由新能力（新硬件/新范式）驱动」的课题——那正是语料外的东西。所以"必然"是错的，正确表述是"**大概率**、且**必须核查**"。

**问题（禁令过宽）**：文档说 agent 的角色是「实验执行器 + 异常记录器 + 证伪器 + 占位核查器」，唯独没有「假设提出者」。但第 1.4 节又要求 agent 设计证伪实验——**设计证伪实验就是在提出假设**。文档一边禁止，一边要求，规则不自洽。

**代价不对称**：在实验便宜、反馈快的领域（ML 系统），「禁止无数据新方向」接近零成本；在实验昂贵、反馈慢的领域（理论、临床、硬件），它会永久堵死所有课题，因为那里永远是假设先行、实验后验。

**改法（可直接替换）**：
> - 你可以**提出**方向与假设（包括从第一性原理推出来的），但：
>   1. **不得把你的假设当作结论**——任何"新方向"在给出「验证计划 + 成本上界 + 停止判据」之前，只是待核查候选，不得写成课题；
>   2. 每个假设必须先在占位核查里被检索一次（§1.3）——因为"必然已被占"虽不成立，"大概率已被占"成立；
>   3. 从语料推出的假设**只能当"待检验预测"**，其最终证据必须来自你或他人跑出的实验；
>   4. 不得用"第一性原理"论证结论，只能用实验数据论证结论。
> **禁止的真实内容**：在没有实验数据时，把任何方向写成结论、推荐或课题卡。

### G2｜第 2 节：核心假设被表述为定律

**原文**：
> **第一代几乎都只优化"速度"，用吞吐/延迟/接受率当判据——这三个判据对"静默输出改变、数值漂移、可复现性"集体失明。**

**问题**：这是一个关于文献分布的实证断言，支撑它的样本只有 `C1` 一个实验环境，且没有引用。它至少在两类情况下失效：
- 领域敏感性：在数据库/编译器等系统领域，第一代工作就把正确性作为默认判据（事务、语义等价、回归测试），"集体失明"不成立；在 LLM 推理服务里它才成立。
- 时间敏感性：测量、正确性、可复现性在 2023–2025 已是显学赛道（可复现性、评测污染、隐藏口径的批评性文献持续增加，参见文末材料）。也就是说，**这条"缝"本身可能已经被抢**，而这恰好违反文档自己「不要挤被抢疯的主赛道」的告诫。

**改法**：
> 把 §2 的这句话改写为**待检验假设 H1**：在当前子领域 S 中，第一代工作是否以"速度类判据"为主、且对正确性/可复现性类判据失明？
> - 检验方式：抽该子领域最近 N 篇代表作，统计其主指标分布（多少篇以吞吐/延迟/加速比为唯一主结果）；
> - H1 成立 → 该子领域走"测量/正确性/可复现性"方向；
> - H1 不成立 → 该子领域必须另找缝隙；
> - 并且**这条假设必须和占位核查一起用**：缝隙被忽视 ≠ 缝隙没人做。

**为什么不删而改**：这条假设其实是全文最有生产力的一句，也最容易被当成公理滥用。降级成假设 + 附上检验步骤，是把它从"信仰"变成"探照灯"的唯一办法。

### G3｜第 1.1→1.2 节之间：循环缺少「复现/噪声底线」

**问题**：循环写作「跑实验 → 记录带数字的异常 → 收敛成一条量化陈述」，中间没有**复现**这一步。观察到一次差异不等于差异存在。LLM 推理实验的噪声源至少包括：逐次运行的非确定性（采样、调度、批拼装）、浮点/归约顺序非确定性、缓存与热状态、内核自动调优、热节流、以及吞吐/延迟测量窗口的选择。**"失效 / 反常 / 度量不可靠 / 不可测"四类异常里，有相当比例是逐次运行的波动**——而整个循环建立在"异常为真"的假设上。

**改法：在第 1.2 节前插入一节（建议编号 1.2，原 1.2 顺延）**：
> **1.2 噪声底线（进入"量化陈述"前的强制门）**
> 1. 任何要做成陈述的异常，必须在**同一配置下重复至少 N 次**（N 按成本设，最低 3 次），报**分布**而不是单点值：中位数 + 离散度 + 极值；
> 2. "反常"必须说明**效应量相对噪声底**的倍数（例如：观测差 12%，run-to-run 波动 3%，则 4×），不足 3× 的不得称为反常；
> 3. 一次性异常记为"噪声待查"，可归档但不得进入 §1.3 占位核查；
> 4. 按成本给判据：复现门通过前投入的算力，计入 §5 的停止规则。

**理由**：没有这条门，agent 会用噪声喂满整条流水线——占位核查、最便宜证伪都会被浪费在假异常上，而且失败原因会被误判成"方向被占"，从而系统性错杀真空位。这是本方法最贵的一种失效模式。

---

## 3. 该补项

### M1｜第 2 节：两个价值理论未合并

文档同时给了两个"空位来源"：
- (a) 「真正的空位 = 第一代已被做，但问题没被解决」——要求读过占据文献并证明它**没解决**（在 §2 开头）；
- (b) 「首选方向是被忽视的缝（测量/正确性/可复现性）」——只要求证明**角度被忽视**（在 §2 结尾）。

两者门槛不同：前者要审查他人结果，后者只要证明没人从这个角度做过。混在一起会让 agent 用 (b) 的便宜标准冒领 (a) 的高价值结论。

**改法**：明确成两套判据并给优先级——**(a) 是上位判据（高价值，成本高，必须读占据文献）；(b) 是下位启发式（低成本，但必须叠加占位核查）**。另外把"首选方向"改称"历史高回报的搜索偏置"，避免 agent 把它当作永远正确的方向。

### M2｜第 4 节的漏洞：先证伪自己

「度量不可靠」被列为需要记录的异常类型之一，这很好；但文档缺少一个**前置**步骤：**在把某个异常当作发现之前，先证伪它是不是自己流程的产物**（空桩、口径漂移、环境差异）。这条不补，整个工作法会被自己的测量误差毒死。

**改法：第 4 节后加清单**
> **自证伪前置检查（每个"反常"及以上级别的异常都要回答）**
> 1. 度量路径里是否存在空桩/占位实现（返回 None、默认值、常量、未通电的分支）被当成真实值？
> 2. 对照组是否真的只差一个变量？
> 3. 异常能否由环境差异解释（版本、驱动、并行度、缓存命中状态、机器负载）？
> 4. 报告数字能否从原始日志独立重算出来？
> 四问全部通过，才允许把该异常称为"发现"；否则降级为"待查缺陷"。

### M3｜第 1.3 节：时间预算与结论粒度不匹配

**自相矛盾**：§1.3 要求「先判断是哪一种：框架被占还是问题被解决」，但同一节的输出被限制为二元「框架已占 / 框架未被占」。**"问题是否被解决"这个判定在输出里没有位置**——也就是文档自己指定的关键判断，在自己的输出格式里无法表达。

**资源冲突**：用「约 1 小时 + 三通道检索」无法判定"问题是否被解决"——那需要读懂占据文献的方法与评测设计。而"在审稿件库"通常不可公开检索，闭源/工业内部工作完全不可见，因此**任何"未占"都只是下限**。

**改法**：
> - 拆两阶段：**存在性检索**（≤1h，三通道）→ 输出三态 **①无人做 / ②有人做且只有框架或局部结果 / ③有人做且有定量结论**；
> - **深入核查**（较长预算，仅对 ②③ 触发）→ 回答"占据了框架的人解决了问题吗"，并允许"部分占"这一结论；
> - 若无法访问在审/闭源层，结论必须写成「**在本 agent 可见范围内未被占**」，不得写成「未被占」——把检索盲区误当空位是最危险的一次误判。

### M4｜第 7 节：「搜索永远只是杀伤工具」

这句话混淆了**工具的能力**与**工具的用途**，而且低估了手上的东西：agent 的检索能做到实时（超出语料截止）、跨语言、跨社区、长尾覆盖。真正诚实的表述是分层：

> - 搜索能判定：**"到目前为止、在公开可检索范围内、以这些关键词，是否有人公开占位"**；
> - 搜索不能判定：**"无人想到"**（沉默不是证据）、**在审/闭源层**、以及**新颖性的最终裁决**；
> - 因此：搜索的"未占"结论**只在可检索范围内有效**（与 M3 的措辞要求一致）；而"搜索能否作探照灯"取决于检索通道——**引用图谱是回溯性的（只看到已被引用的），预印本与实时检索是前瞻性的**。文档把两者一起判成"只是杀伤工具"，等于自己砍掉了 §7 里承认的那条"接到实时前沿"的唯一通路。

### M5｜第 4 节：定义过窄 + 记录格式缺失

**定义过窄**：§1.1 把「度量不可靠」限定为「口径/代理量/环境依赖可疑」，但该类别实际至少有两轴，混在一格里会让 agent 漏记：
- **轴 A｜定义问题**：代理量（用 A 代替 B）、口径（统计窗口/denominator/是否含预热）；
- **轴 B｜实现问题**：**空桩**（返回值恒定/始终 None）、**未通电分支**、**默认值冒充实测**。

`C1` 的例子（`cached_tokens` 是恒返回 None 的空桩）属于轴 B，但用轴 A 的定义描述会让人以为那是"口径选择"。

**记录格式缺失**：§4 说「任何将写入结论的数字，必须能追溯到采集方式、口径、环境条件、配置点」——这是原则，不是格式。agent 无法据此产出可审计记录。建议强制字段：

| 字段 | 说明 |
|---|---|
| 指标名 | 精确定义 + 单位 |
| 采集位置 | 代码路径 / 日志字段（原始日志需可独立重算） |
| 采集命令 | 可复现的命令行 |
| 窗口与样本量 | 统计起止、请求数、是否含预热 |
| 配置点 | 全部相关开关与取值 |
| 环境 | 版本 / 驱动 / 硬件 / 负载 |
| 敏感性 | 在哪些参数轴上扫过；**未扫的必须显式写"未做敏感性扫描"** |
| 噪声底 | run-to-run 分布（由 G3 提供） |

### M6｜缺少「升级与算力规则」

整个循环没有人工升级点。§5 的停止规则是封存规则，但没有任何地方规定：**什么时候该让人来判断**。按现在的写法，agent 可以自主把候选封存掉（把课题封掉的决定，成本很高且不可逆），也可以自主无限重跑（每次证伪 ≤1 天，但没有总数上限）。

**改法：§5 后加一节**
> - 单候选上限：算力上界 / 时间上界 / 证伪实验次数上限（例如 3 次），超限强制升级给人类，不允许 agent 自行封存；
> - 连续 K 个候选全部死于证伪 → 停下并报告"本环境当前不产出空位"，而不是继续换方向硬找；
> - 停止规则的两类区分：**①数据容量耗尽（封存）**；**②测量不可靠（必须换度量后重跑，不得封存）**——现文只有①。

---

## 4. 可选改进（低成本，收益明确）

- **L1 术语统一**：§1.1 表格用「度量不可靠」，§4 用「隐性口径」，指同一类，建议统一为一个术语并给出两个轴（见 M5）。
- **L2 未定义专有名词**：`C1`、`dflash 臂`、`PC 开关`、以及表格里 `ValidationError` 的上下文，对文档外部的 agent 不可解。要么加术语表，要么补最小背景。文档定位是"给 agent 的操作手册"，这一点是硬要求。
- **L3 「各一条」会诱发编造**：§1.1 要求四类异常「各写一句」，但表格示例本身并不齐。应写明「没有该类异常时写『本回合无』」——否则 agent 会为了填表把噪声包装成异常（与 G3 联动）。
- **L4 缺可套用模板**：§6 是散文式指令，缺少 §6 自己承诺的「课题卡」的字段。建议补三张模板：**异常记录卡 / 占位核查结论卡 / 课题卡**。课题卡最低字段：异常（带数字）→ 量化陈述 → 基线/参照系 → 空位判定（含检索范围与时间戳）→ 最便宜证伪方案 → 判据（继续/停止）→ 已知风险与未知变量。
- **L5 分级无判定程序**：§3 的分级轴很好，但没有判定步骤。建议程序化：①这个异常会改变哪个**具体**结论？②持有那个结论的人的默认假设是什么？③若有第三方仍能得到同样结论，则不是高级异常。
- **L6 §2 的全称量词无支撑**：「第一代几乎都只优化速度」应补可核验材料。检索中可用的公开例子：`cached_tokens` 类使用量字段未上报/为空的问题（[vLLM #17519](https://github.com/vllm-project/vllm/pull/17519)、[vllm-ascend #13040](https://github.com/vllm-project/vllm-ascend/pull/13040)）、接受率与输出分布/模板相关的报告（[ROCm/ATOM #609](https://github.com/ROCm/ATOM/issues/609)）、以及关于推理进展可复现性的批评性综述（[可复现性的挑战与路径](https://www.chatpaper.ai/zh/dashboard/paper/b4862e9d-6405-42cf-b776-7abbd64ce3ee)）。把它们作为 H1 的证据起点，比空口全称断言强得多。
- **L7 参数敏感性归类错位**：§4 把「参数相关的结论」列为隐性口径，但它本质属于 §1.1 的「反常/度量不可靠」交叠区（`C1` 的 Phase 1→3 反转即此例）。建议提升为独立的第 4 类口径陷阱，因为它最容易被忽略且代价最大。
- **L8 缺少「实验做完但零异常」的出口**：若 agent 跑了大量实验却没有异常，现文没有规定该做什么（是换实验设计、还是升级、还是封存）。建议加一句：连续无异常 → 检查**度量分辨率**是否足以看见差异，而不是直接判定"没问题"。

---

## 5. 一个值得讨论的设计问题（不必立即改）

循环的出口只有「停止规则」（封存）。但在真实研究里，课题常常需要**换一个更贵的实验**（换硬件、换规模、换真实负载），而不是被证伪。现文没有处理"数据的问题容量已尽，但**扩大数据**能继续"这种情况。可以考虑在 §5 增加第三类：**③扩容型停止——当前数据容量耗尽，但已明确下一步需要什么新变量/新规模**，这类不是封存，而是转入资源申请。否则按现文，agent 会把"缺数据"误判成"问题已死"。

---

## 6. 如果要我直接动手，我建议的最小改动集

1. 第 0 节禁令改写（G1）——**约 10 行**；
2. 第 1.2 节前插入「噪声底线」并以复现门重排编号（G3）——**约 12 行**；
3. 第 2 节改写为「假设 H1 + 检验步骤 + 两套判据的优先级」（G2 + M1）——**约 15 行**；
4. 第 1.3 节拆两阶段、改三态输出（M3）——**约 10 行**；
5. 第 4 节补字段表与自证伪清单（M2 + M5）——**约 20 行**；
6. 第 5 节后加「升级与算力规则」（M6）——**约 8 行**；
7. 第 6 节补三张模板（L4）——**约 30 行**。

七处改完，文档从"立场鲜明的宣言"变成"可交给 agent 直接执行的手册"，且**不损失它原有的锐度**——锐度来自 G1/G2/G3 的表述收紧，而不是来自绝对化措辞。

---

## 附：审核中使用的核验材料

- [vLLM PR #17519：为请求输出增加 num_cached_tokens 统计](https://github.com/vllm-project/vllm/pull/17519)（说明使用量字段长期缺失/需补报）
- [vllm-ascend PR #13040：在 PD proxy 中上报 prefiller cached tokens](https://github.com/vllm-project/vllm-ascend/pull/13040)（同上，跨实现差异）
- [ROCm/ATOM Issue #609：为什么 chat 模板会降低 DeepSeek MTP 推测解码接受率](https://github.com/ROCm/ATOM/issues/609)（接受率与输出分布/模板耦合，支持"接受率是失效判据"的判断）
- [理性审视语言模型推理进展：可复现性的挑战与路径](https://www.chatpaper.ai/zh/dashboard/paper/b4862e9d-6405-42cf-b776-7abbd64ce3ee)（可复现性已成显学赛道的证据，用于 M1）
